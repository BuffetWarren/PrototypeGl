@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Membre {{ $membre->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/admin/membres') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/admin/membres/' . $membre->id . '/edit') }}" title="Edit Membre"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/membres', $membre->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-sm',
                                    'title' => 'Delete Membre',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $membre->id }}</td>
                                    </tr>
                                    <tr><th> Nom </th><td> {{ $membre->nom }} </td></tr><tr><th> Prenom </th><td> {{ $membre->prenom }} </td></tr><tr><th> Login </th><td> {{ $membre->login }} </td></tr><tr><th> Email </th><td> {{ $membre->email }} </td></tr><tr><th> Ville </th><td> {{ $membre->ville }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
